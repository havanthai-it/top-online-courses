import { connect } from 'react-redux';
import SubscribeDialogView from './SubscribeDialog.view';

const mapStateToProps = (state) => {
  return {
  }
}

const mapDispatchToProps = (dispatch) => {
  return {
  }
}

const SubscribeDialog = connect(mapStateToProps, mapDispatchToProps)(SubscribeDialogView);

export default SubscribeDialog;