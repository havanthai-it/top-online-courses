import { connect } from 'react-redux';
import ListCoursesHorizontalView from './ListCoursesHorizontal.view';

const mapStateToProps = (state) => {
  return {

  };
}

const mapDispatchToProps = (dispatch) => {
  return {

  };
}

const ListCoursesHorizontal = connect(mapStateToProps, mapDispatchToProps)(ListCoursesHorizontalView);

export default ListCoursesHorizontal;
