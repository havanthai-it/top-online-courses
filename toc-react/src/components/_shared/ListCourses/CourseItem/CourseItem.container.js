import { connect } from 'react-redux';
import CourseItemView from './CourseItem.view';

const mapStateToProps = (state) => {
  return {

  };
}

const mapDispatchToProps = (dispatch) => {
  return {

  };
}

const CourseItem = connect(mapStateToProps, mapDispatchToProps)(CourseItemView);

export default CourseItem;
