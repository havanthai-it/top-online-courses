import { connect } from 'react-redux';
import TabListCoursesHorizontalView from './TabListCoursesHorizontal.view';

const mapStateToProps = (state) => {
  return {

  };
}

const mapDispatchToProps = (dispatch) => {
  return {

  };
}

const TabListCoursesHorizontal = connect(mapStateToProps, mapDispatchToProps)(TabListCoursesHorizontalView);

export default TabListCoursesHorizontal;
