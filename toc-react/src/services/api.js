const api = {};

const base_url = 'http://localhost:4000/api/v1';

api.category = `${base_url}/category`;

api.platform = `${base_url}/platform`;

api.course = `${base_url}/course`;

export default api;